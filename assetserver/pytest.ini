[pytest]
testpaths = tests
pythonpath = src
asyncio_mode = strict
addopts = -v --tb=short --strict-markers
markers =
    integration: Tests that require external services (ComfyUI, etc.)
    unit: Fast unit tests with no external dependencies
    slow: Tests that take longer than 5 seconds

